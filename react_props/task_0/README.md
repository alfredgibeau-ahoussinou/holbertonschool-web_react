# Task 0 - Component Extraction

- Components: `Header`, `Footer`, `Login`; `Notifications` kept.
- `App` renders all inside a React Fragment.

## Commands

From `task_0/dashboard`:

- Install: `npm ci`
- Test: `npm test -- --watchAll=false`
- Dev: `npm run dev`
