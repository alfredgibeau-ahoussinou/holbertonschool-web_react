# Task 4 - Context consumer & advanced state

Objectifs:
- `Footer` consomme le contexte (fonctionnel): affiche "Contact us" si connecté
- `App` gère `notifications` et `courses`
- `markNotificationAsRead(id)` supprime la notif et log le message
- `Notifications` devient `PureComponent`
- Tests adaptés

Lancer les tests:
```
cd react_state/task_4/dashboard && npm i && npm test -i
```

