# Task 2 - Context

Objectifs:
- Créer un contexte (user + logOut)
- Gérer `user` dans `App` avec `logIn`/`logOut`
- Fournir le provider à l'application
- `Login` appelle `logIn(email, password)`

Lancer les tests:
```
cd react_state/task_2/dashboard && npm i && npm test -i
```

