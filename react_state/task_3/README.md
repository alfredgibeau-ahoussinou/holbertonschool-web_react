# Task 3 - Log Out implementation

Objectifs:
- `Header` consomme le contexte via `contextType`
- Afficher `logoutSection` si connecté
- Clic sur `logout` appelle `logOut`
- Tests `Header` et ajustements `App`

Lancer les tests:
```
cd react_state/task_3/dashboard && npm i && npm test -i
```

