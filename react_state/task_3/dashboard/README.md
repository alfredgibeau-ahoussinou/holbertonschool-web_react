# Task 3 — Dashboard

Logout via `Header` consommant le contexte avec `contextType`.

## Scripts

- Installer: `npm i`
- Développement: `npm run dev`
- Tests: `npm test -i`
- Lint: `npm run lint`
- Build: `npm run build`
- Preview: `npm run preview`

## Points clés
- `logoutSection` visible seulement si `user.isLoggedIn`.
- Clic sur "logout" appelle `logOut` du contexte.

