# Task 1 - Controlled components & state callback

Objectifs:
- Convertir `Login` en composant de classe
- Champs contrôlés `email` et `password`
- Validation: email valide + mot de passe ≥ 8
- `enableSubmit` active le bouton Submit

Lancer les tests:
```
cd react_state/task_1/dashboard && npm i && npm test -i
```

