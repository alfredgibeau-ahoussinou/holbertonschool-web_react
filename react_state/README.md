# React State Project

Ce dossier contient les tâches `task_0` à `task_4` autour de l'état, des formulaires, du Context et de tests.

Chaque dossier `task_X/dashboard` est une app Vite avec Jest configuré.

